# Supply credentials through the host's secret environment variables.
tmdb.apikey = ${?TMDB_API_KEY}
play.http.secret.key = ${?APPLICATION_SECRET}
play.filters.hosts.allowed = ["localhost", "127.0.0.1"]
play.filters.hosts.allowed += ${?APP_HOST}
play.filters.hosts.allowed += ${?RENDER_EXTERNAL_HOSTNAME}
play.server.http.idleTimeout = 1h
pekko.http.server.idle-timeout = 1h
